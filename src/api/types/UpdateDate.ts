/**
 * @desc Used for comparing dates stored by users phone to that on server
 * @author LocalNewsTV
 */
type UpdateDate = {
  dateModified: string;
  serviceType: string;
};

export default UpdateDate;
